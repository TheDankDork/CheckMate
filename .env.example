# Copy this file to .env and fill in your own values.
# Never commit .env — it is in .gitignore.

# Backend (CheckMate API)
# Important: Create the key in a NEW project (aistudio.google.com → Create API key)
# so you get a separate quota. Keys from the same project share the same limit.
GEMINI_API_KEY=your_gemini_api_key_here
GEMINI_MODEL=gemini-2.0-flash
PORT=5000

# Frontend (optional; only if pointing at a different backend)
# VITE_API_BASE_URL=http://localhost:5000
